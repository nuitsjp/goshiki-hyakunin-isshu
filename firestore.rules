rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ユーザーは自分のクイズセッションデータのみアクセス可能
    match /users/{userId}/quizSessions/{sessionId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
